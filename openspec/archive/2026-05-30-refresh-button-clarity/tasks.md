## 1. Icon

- [x] 1.1 Import `ArrowsClockwise` from `@phosphor-icons/react` in `src/renderer/components/Icon.tsx`
- [x] 1.2 Add `'arrows-clockwise': ArrowsClockwise` to `ICON_MAP` in `Icon.tsx`

## 2. Header

- [x] 2.1 Replace `icon={<Icon name="play" size={15} />}` with `icon={<Icon name="arrows-clockwise" size={15} />}` on the refresh `Button` in `Header.tsx`
- [x] 2.2 Add the label `Refresh` as the button's children so it renders as a labelled button instead of icon-only
